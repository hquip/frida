diff --git a/inject/frida-inject.xcent b/inject/frida-inject.xcent
index ecbeb720..3b40dd96 100644
--- a/inject/frida-inject.xcent
+++ b/inject/frida-inject.xcent
@@ -7,7 +7,7 @@
 	<key>adi-client</key>
 	<string>166430402</string>
 	<key>application-identifier</key>
-	<string>re.frida.Server</string>
+	<string>re.fs.Server</string>
 	<key>aps-connection-initiate</key>
 	<true/>
 	<key>auth-key-vault-client</key>
diff --git a/inject/meson.build b/inject/meson.build
index b9b67606..2918c20b 100644
--- a/inject/meson.build
+++ b/inject/meson.build
@@ -31,7 +31,7 @@ raw_inject = executable('frida-inject-raw', inject_sources,
 custom_target('frida-inject',
   input: [raw_inject, 'frida-inject.xcent'],
   output: 'frida-inject' + exe_suffix,
-  command: post_process + ['executable', 're.frida.Inject', '@INPUT1@'],
+  command: post_process + ['executable', 're.fs.Inject', '@INPUT1@'],
   install: true,
   install_dir: get_option('bindir'),
 )
diff --git a/lib/base/session.vala b/lib/base/session.vala
index f39543a2..51b3928a 100644
--- a/lib/base/session.vala
+++ b/lib/base/session.vala
@@ -1,5 +1,5 @@
 namespace Frida {
-	[DBus (name = "re.frida.HostSession17")]
+	[DBus (name = "re.fs.HostSession17")]
 	public interface HostSession : Object {
 		public abstract async void ping (uint interval_seconds, Cancellable? cancellable) throws GLib.Error;
 
@@ -43,7 +43,7 @@ namespace Frida {
 		public signal void uninjected (InjectorPayloadId id);
 	}
 
-	[DBus (name = "re.frida.AgentSessionProvider17")]
+	[DBus (name = "re.fs.AgentSessionProvider17")]
 	public interface AgentSessionProvider : Object {
 		public abstract async void open (AgentSessionId id, HashTable<string, Variant> options,
 			Cancellable? cancellable) throws GLib.Error;
@@ -58,7 +58,7 @@ namespace Frida {
 		public signal void child_gating_changed (uint subscriber_count);
 	}
 
-	[DBus (name = "re.frida.AgentSession17")]
+	[DBus (name = "re.fs.AgentSession17")]
 	public interface AgentSession : Object {
 		public abstract async void close (Cancellable? cancellable) throws GLib.Error;
 
@@ -100,7 +100,7 @@ namespace Frida {
 		public signal void candidate_gathering_done ();
 	}
 
-	[DBus (name = "re.frida.AgentController17")]
+	[DBus (name = "re.fs.AgentController17")]
 	public interface AgentController : Object {
 #if !WINDOWS
 		public abstract async HostChildId prepare_to_fork (uint parent_pid, Cancellable? cancellable, out uint parent_injectee_id,
@@ -121,7 +121,7 @@ namespace Frida {
 			Cancellable? cancellable) throws GLib.Error;
 	}
 
-	[DBus (name = "re.frida.AgentMessageSink17")]
+	[DBus (name = "re.fs.AgentMessageSink17")]
 	public interface AgentMessageSink : Object {
 		public abstract async void post_messages (AgentMessage[] messages, uint batch_id,
 			Cancellable? cancellable) throws GLib.Error;
@@ -749,7 +749,7 @@ namespace Frida {
 		}
 	}
 
-	[DBus (name = "re.frida.GadgetSession17")]
+	[DBus (name = "re.fs.GadgetSession17")]
 	public interface GadgetSession : Object {
 		public abstract async void break_and_resume (Cancellable? cancellable) throws GLib.Error;
 		public abstract async void break_and_detach (Cancellable? cancellable) throws GLib.Error;
@@ -762,14 +762,14 @@ namespace Frida {
 		PAGE_PLAN,
 	}
 
-	[DBus (name = "re.frida.Channel17")]
+	[DBus (name = "re.fs.Channel17")]
 	public interface Channel : Object {
 		public abstract async void close (Cancellable? cancellable) throws GLib.Error;
 		public abstract async void input (uint8[] data, Cancellable? cancellable) throws GLib.Error;
 		public signal void output (uint8[] data);
 	}
 
-	[DBus (name = "re.frida.ServiceSession17")]
+	[DBus (name = "re.fs.ServiceSession17")]
 	public interface ServiceSession : Object {
 		public signal void close ();
 		public signal void message (Variant message);
@@ -779,13 +779,13 @@ namespace Frida {
 		public abstract async Variant request (Variant parameters, Cancellable? cancellable) throws GLib.Error;
 	}
 
-	[DBus (name = "re.frida.TransportBroker17")]
+	[DBus (name = "re.fs.TransportBroker17")]
 	public interface TransportBroker : Object {
 		public abstract async void open_tcp_transport (AgentSessionId id, Cancellable? cancellable, out uint16 port,
 			out string token) throws GLib.Error;
 	}
 
-	[DBus (name = "re.frida.PortalSession17")]
+	[DBus (name = "re.fs.PortalSession17")]
 	public interface PortalSession : Object {
 		public abstract async void join (HostApplicationInfo app, SpawnStartState current_state,
 			AgentSessionId[] interrupted_sessions, HashTable<string, Variant> options, Cancellable? cancellable,
@@ -794,14 +794,14 @@ namespace Frida {
 		public signal void kill ();
 	}
 
-	[DBus (name = "re.frida.BusSession17")]
+	[DBus (name = "re.fs.BusSession17")]
 	public interface BusSession : Object {
 		public abstract async void attach (Cancellable? cancellable) throws GLib.Error;
 		public abstract async void post (string json, bool has_data, uint8[] data, Cancellable? cancellable) throws GLib.Error;
 		public signal void message (string json, bool has_data, uint8[] data);
 	}
 
-	[DBus (name = "re.frida.AuthenticationService17")]
+	[DBus (name = "re.fs.AuthenticationService17")]
 	public interface AuthenticationService : Object {
 		public abstract async string authenticate (string token, Cancellable? cancellable) throws GLib.Error;
 	}
@@ -1047,7 +1047,7 @@ namespace Frida {
 		}
 	}
 
-	[DBus (name = "re.frida.Error")]
+	[DBus (name = "re.fs.Error")]
 	public errordomain Error {
 		SERVER_NOT_RUNNING,
 		EXECUTABLE_NOT_FOUND,
@@ -2129,18 +2129,18 @@ namespace Frida {
 	}
 
 	namespace ObjectPath {
-		public const string HOST_SESSION = "/re/frida/HostSession";
-		public const string AGENT_SESSION_PROVIDER = "/re/frida/AgentSessionProvider";
-		public const string AGENT_SESSION = "/re/frida/AgentSession";
-		public const string AGENT_CONTROLLER = "/re/frida/AgentController";
-		public const string AGENT_MESSAGE_SINK = "/re/frida/AgentMessageSink";
-		public const string GADGET_SESSION = "/re/frida/GadgetSession";
-		public const string CHANNEL = "/re/frida/Channel";
-		public const string SERVICE = "/re/frida/Service";
-		public const string TRANSPORT_BROKER = "/re/frida/TransportBroker";
-		public const string PORTAL_SESSION = "/re/frida/PortalSession";
-		public const string BUS_SESSION = "/re/frida/BusSession";
-		public const string AUTHENTICATION_SERVICE = "/re/frida/AuthenticationService";
+		public const string HOST_SESSION = "/re/fs/HostSession";
+		public const string AGENT_SESSION_PROVIDER = "/re/fs/AgentSessionProvider";
+		public const string AGENT_SESSION = "/re/fs/AgentSession";
+		public const string AGENT_CONTROLLER = "/re/fs/AgentController";
+		public const string AGENT_MESSAGE_SINK = "/re/fs/AgentMessageSink";
+		public const string GADGET_SESSION = "/re/fs/GadgetSession";
+		public const string CHANNEL = "/re/fs/Channel";
+		public const string SERVICE = "/re/fs/Service";
+		public const string TRANSPORT_BROKER = "/re/fs/TransportBroker";
+		public const string PORTAL_SESSION = "/re/fs/PortalSession";
+		public const string BUS_SESSION = "/re/fs/BusSession";
+		public const string AUTHENTICATION_SERVICE = "/re/fs/AuthenticationService";
 
 		public static string for_agent_session (AgentSessionId id) {
 			return AGENT_SESSION + "/" + id.handle;
diff --git a/lib/gadget/gadget.vala b/lib/gadget/gadget.vala
index 230284c7..89fc2956 100644
--- a/lib/gadget/gadget.vala
+++ b/lib/gadget/gadget.vala
@@ -1724,7 +1724,7 @@ namespace Frida.Gadget {
 				}
 
 				uint pid = get_process_id ();
-				string identifier = "re.frida.Gadget";
+				string identifier = "re.fs.Gadget";
 				string name = "Gadget";
 				var no_parameters = make_parameters_dict ();
 				this_app = HostApplicationInfo (identifier, name, pid, no_parameters);
diff --git a/lib/netif/tunnel-interface-observer.vala b/lib/netif/tunnel-interface-observer.vala
index 4325d0ad..b6e2c17d 100644
--- a/lib/netif/tunnel-interface-observer.vala
+++ b/lib/netif/tunnel-interface-observer.vala
@@ -4,7 +4,7 @@ public sealed class Frida.TunnelInterfaceObserver : Object, DynamicInterfaceObse
 
 	private Darwin.SystemConfiguration.DynamicStore? store;
 	private Darwin.GCD.DispatchQueue event_queue =
-		new Darwin.GCD.DispatchQueue ("re.frida.endpoint-enumerator", Darwin.GCD.DispatchQueueAttr.SERIAL);
+		new Darwin.GCD.DispatchQueue ("re.fs.endpoint-enumerator", Darwin.GCD.DispatchQueueAttr.SERIAL);
 
 	private MainContext? main_context;
 
diff --git a/portal/meson.build b/portal/meson.build
index 16e91008..78ea691b 100644
--- a/portal/meson.build
+++ b/portal/meson.build
@@ -29,7 +29,7 @@ raw_portal = executable('frida-portal-raw', portal_sources,
 custom_target('frida-portal',
   input: [raw_portal, 'frida-portal.xcent'],
   output: 'frida-portal' + exe_suffix,
-  command: post_process + ['executable', 're.frida.Portal', '@INPUT1@'],
+  command: post_process + ['executable', 're.fs.Portal', '@INPUT1@'],
   install: true,
   install_dir: get_option('bindir'),
 )
diff --git a/releng b/releng
--- a/releng
+++ b/releng
@@ -1 +1 @@
-Subproject commit 800f2385c3e8faddecc8da58f8572ef34605fdce
+Subproject commit 800f2385c3e8faddecc8da58f8572ef34605fdce-dirty
diff --git a/server/frida-server.plist b/server/frida-server.plist
index 1580573b..f3b29e4c 100644
--- a/server/frida-server.plist
+++ b/server/frida-server.plist
@@ -5,7 +5,7 @@
 	<key>CFBundleDevelopmentRegion</key>
 	<string>English</string>
 	<key>CFBundleIdentifier</key>
-	<string>re.frida.Server</string>
+	<string>re.fs.Server</string>
 	<key>CFBundleInfoDictionaryVersion</key>
 	<string>6.0</string>
 	<key>CFBundleName</key>
diff --git a/server/frida-server.xcent b/server/frida-server.xcent
index 19b54260..e416d992 100644
--- a/server/frida-server.xcent
+++ b/server/frida-server.xcent
@@ -13,7 +13,7 @@
 	<key>adi-client</key>
 	<string>166430402</string>
 	<key>application-identifier</key>
-	<string>re.frida.Server</string>
+	<string>re.fs.Server</string>
 	<key>aps-connection-initiate</key>
 	<true/>
 	<key>auth-key-vault-client</key>
diff --git a/server/meson.build b/server/meson.build
index bd55156d..21b0daa4 100644
--- a/server/meson.build
+++ b/server/meson.build
@@ -42,7 +42,7 @@ need_universal_server = get_option('assets') == 'installed' and server_compat !=
 server = custom_target('frida-server',
   input: [raw_server, 'frida-server.xcent'],
   output: need_universal_server ? f'@server_name@-@host_abi@' : server_name,
-  command: post_process + ['executable', 're.frida.Server', '@INPUT1@'],
+  command: post_process + ['executable', 're.fs.Server', '@INPUT1@'],
   install: true,
   install_dir: get_option('bindir'),
 )
diff --git a/server/server.vala b/server/server.vala
index 67bf4b38..94b3884c 100644
--- a/server/server.vala
+++ b/server/server.vala
@@ -1,7 +1,7 @@
 namespace Frida.Server {
 	private static Application application;
 
-	private const string DEFAULT_DIRECTORY = "re.frida.server";
+	private const string DEFAULT_DIRECTORY = "re.fs.server";
 	private static bool output_version = false;
 	private static string? device_id = null;
 	private static string? listen_address = null;
diff --git a/src/agent-container.vala b/src/agent-container.vala
index eecfa1f6..05f3359f 100644
--- a/src/agent-container.vala
+++ b/src/agent-container.vala
@@ -28,7 +28,7 @@ namespace Frida {
 			}
 
 			void * main_func_symbol;
-			var main_func_found = container.module.symbol ("frida_agent_main", out main_func_symbol);
+			var main_func_found = container.module.symbol ("bads_agent_main", out main_func_symbol);
 			assert (main_func_found);
 			container.main_impl = (AgentMainFunc) main_func_symbol;
 
@@ -103,7 +103,7 @@ namespace Frida {
 		}
 
 		private void start_worker_thread () {
-			thread = new Thread<bool> ("frida-agent-container", run);
+			thread = new Thread<bool> ("bads-agent-container", run);
 		}
 
 		private void stop_worker_thread () {
diff --git a/src/darwin/darwin-host-session.vala b/src/darwin/darwin-host-session.vala
index 83214a5b..2a6ad5e7 100644
--- a/src/darwin/darwin-host-session.vala
+++ b/src/darwin/darwin-host-session.vala
@@ -91,7 +91,7 @@ namespace Frida {
 			injector.uninjected.connect (on_uninjected);
 
 #if HAVE_EMBEDDED_ASSETS
-			var blob = Frida.Data.Agent.get_frida_agent_dylib_blob ();
+			var blob = Frida.Data.Agent.get_bads_agent_dylib_blob ();
 			agent = new AgentResource (blob.name, new Bytes.static (blob.data), tempdir);
 #endif
 
@@ -307,7 +307,7 @@ namespace Frida {
 		private async uint inject_agent (uint pid, string agent_parameters, Cancellable? cancellable) throws Error, IOError {
 			uint id;
 
-			unowned string entrypoint = "frida_agent_main";
+			unowned string entrypoint = "bads_agent_main";
 #if HAVE_EMBEDDED_ASSETS
 			id = yield fruitjector.inject_library_resource (pid, agent, entrypoint, agent_parameters, cancellable);
 #else
diff --git a/src/darwin/frida-helper-backend-glue.m b/src/darwin/frida-helper-backend-glue.m
index 1f86ac23..90541362 100644
--- a/src/darwin/frida-helper-backend-glue.m
+++ b/src/darwin/frida-helper-backend-glue.m
@@ -419,14 +419,14 @@ static void frida_inject_instance_on_mach_thread_dead (void * context);
 static void frida_inject_instance_join_posix_thread (FridaInjectInstance * self, mach_port_t posix_thread);
 static void frida_inject_instance_on_posix_thread_dead (void * context);
 
-static gboolean frida_agent_context_init (FridaAgentContext * self, const FridaAgentDetails * details, const FridaInjectPayloadLayout * layout,
+static gboolean bads_agent_context_init (FridaAgentContext * self, const FridaAgentDetails * details, const FridaInjectPayloadLayout * layout,
     mach_vm_address_t payload_base, mach_vm_size_t payload_size, GumDarwinModuleResolver * resolver, GumDarwinMapper * mapper, GError ** error);
-static gboolean frida_agent_context_init_functions (FridaAgentContext * self, GumDarwinModuleResolver * resolver, GumDarwinMapper * mapper,
+static gboolean bads_agent_context_init_functions (FridaAgentContext * self, GumDarwinModuleResolver * resolver, GumDarwinMapper * mapper,
     GError ** error);
 
-static void frida_agent_context_emit_mach_stub_code (FridaAgentContext * self, guint8 * code, GumDarwinModuleResolver * resolver,
+static void bads_agent_context_emit_mach_stub_code (FridaAgentContext * self, guint8 * code, GumDarwinModuleResolver * resolver,
     GumDarwinMapper * mapper);
-static void frida_agent_context_emit_pthread_stub_code (FridaAgentContext * self, guint8 * code, GumDarwinModuleResolver * resolver,
+static void bads_agent_context_emit_pthread_stub_code (FridaAgentContext * self, guint8 * code, GumDarwinModuleResolver * resolver,
     GumDarwinMapper * mapper);
 
 static gboolean frida_convert_thread_state_for_task (mach_port_t task, thread_state_flavor_t flavor, gconstpointer in_state,
@@ -767,7 +767,7 @@ _frida_darwin_helper_backend_create_dispatch_context (FridaDarwinHelperBackend *
   FridaDispatchContext * ctx;
 
   ctx = g_slice_new (FridaDispatchContext);
-  ctx->dispatch_queue = dispatch_queue_create ("re.frida.helper.queue", DISPATCH_QUEUE_SERIAL);
+  ctx->dispatch_queue = dispatch_queue_create ("re.fs.helper.queue", DISPATCH_QUEUE_SERIAL);
 
   self->dispatch_context = ctx;
 }
@@ -1896,14 +1896,14 @@ _frida_darwin_helper_backend_prepare_spawn_instance_for_injection (FridaDarwinHe
    * We POSIX_SPAWN_START_SUSPENDED which means that the kernel will create
    * the task and its main thread, with the main thread's instruction pointer
    * pointed at __dyld_start. At this point neither dyld nor libc have been
-   * initialized, so we won't be able to inject frida-agent at this point.
+   * initialized, so we won't be able to inject bads-agent at this point.
    *
    * So here's what we'll do before we try to inject our dylib:
    * - Get hold of the main thread to read its instruction pointer, which will
    *   tell us where dyld is in memory.
    * - Walk backwards to find dyld's Mach-O header.
    * - Walk its symbols and find a function that's called at a point where the process is
-   *   sufficiently initialized to load frida-agent, but still early enough so the app's
+   *   sufficiently initialized to load bads-agent, but still early enough so the app's
    *   initializer(s) didn't get a chance to run.
    * - For processes using dyld v3's closure support we put a hardware breakpoint inside
    *   dyld::launchWithClosure() right after setInitialImageList() has been called.
@@ -2327,12 +2327,12 @@ _frida_darwin_helper_backend_inject_into_task (FridaDarwinHelperBackend * self,
   kr = mach_vm_protect (task, payload_address + layout.stack_guard_offset, layout.stack_guard_size, FALSE, VM_PROT_NONE);
   CHECK_MACH_RESULT (kr, ==, KERN_SUCCESS, "mach_vm_protect");
 
-  if (!frida_agent_context_init (&agent_ctx, &details, &layout, payload_address, instance->payload_size, resolver, mapper, error))
+  if (!bads_agent_context_init (&agent_ctx, &details, &layout, payload_address, instance->payload_size, resolver, mapper, error))
     goto failure;
 
-  frida_agent_context_emit_mach_stub_code (&agent_ctx, mach_stub_code, resolver, mapper);
+  bads_agent_context_emit_mach_stub_code (&agent_ctx, mach_stub_code, resolver, mapper);
 
-  frida_agent_context_emit_pthread_stub_code (&agent_ctx, pthread_stub_code, resolver, mapper);
+  bads_agent_context_emit_pthread_stub_code (&agent_ctx, pthread_stub_code, resolver, mapper);
 
   if (gum_query_is_rwx_supported () || !gum_code_segment_is_supported ())
   {
@@ -4244,7 +4244,7 @@ frida_inject_instance_task_did_not_exec (FridaInjectInstance * instance)
 }
 
 static gboolean
-frida_agent_context_init (FridaAgentContext * self, const FridaAgentDetails * details, const FridaInjectPayloadLayout * layout,
+bads_agent_context_init (FridaAgentContext * self, const FridaAgentDetails * details, const FridaInjectPayloadLayout * layout,
     mach_vm_address_t payload_base, mach_vm_size_t payload_size, GumDarwinModuleResolver * resolver, GumDarwinMapper * mapper, GError ** error)
 {
   bzero (self, sizeof (FridaAgentContext));
@@ -4257,7 +4257,7 @@ frida_agent_context_init (FridaAgentContext * self, const FridaAgentDetails * de
   self->constructed = FALSE;
   self->module_handle = NULL;
 
-  if (!frida_agent_context_init_functions (self, resolver, mapper, error))
+  if (!bads_agent_context_init_functions (self, resolver, mapper, error))
     return FALSE;
 
   self->mach_port_allocate_right = MACH_PORT_RIGHT_RECEIVE;
@@ -4307,7 +4307,7 @@ frida_agent_context_init (FridaAgentContext * self, const FridaAgentDetails * de
   self->field##_impl = gum_strip_code_address (gum_darwin_module_resolver_find_export_address (resolver, module, G_STRINGIFY (field)))
 
 static gboolean
-frida_agent_context_init_functions (FridaAgentContext * self, GumDarwinModuleResolver * resolver, GumDarwinMapper * mapper, GError ** error)
+bads_agent_context_init_functions (FridaAgentContext * self, GumDarwinModuleResolver * resolver, GumDarwinMapper * mapper, GError ** error)
 {
   gboolean success = FALSE;
   GumDarwinModule * module;
@@ -4379,11 +4379,11 @@ beach:
 
 #ifdef HAVE_I386
 
-static void frida_agent_context_emit_mach_stub_body (FridaAgentContext * self, FridaAgentEmitContext * ctx);
-static void frida_agent_context_emit_pthread_stub_body (FridaAgentContext * self, FridaAgentEmitContext * ctx);
+static void bads_agent_context_emit_mach_stub_body (FridaAgentContext * self, FridaAgentEmitContext * ctx);
+static void bads_agent_context_emit_pthread_stub_body (FridaAgentContext * self, FridaAgentEmitContext * ctx);
 
 static void
-frida_agent_context_emit_mach_stub_code (FridaAgentContext * self, guint8 * code, GumDarwinModuleResolver * resolver,
+bads_agent_context_emit_mach_stub_code (FridaAgentContext * self, guint8 * code, GumDarwinModuleResolver * resolver,
     GumDarwinMapper * mapper)
 {
   FridaAgentEmitContext ctx;
@@ -4393,14 +4393,14 @@ frida_agent_context_emit_mach_stub_code (FridaAgentContext * self, guint8 * code
   gum_x86_writer_set_target_cpu (&ctx.cw, resolver->cpu_type);
   ctx.mapper = mapper;
 
-  frida_agent_context_emit_mach_stub_body (self, &ctx);
+  bads_agent_context_emit_mach_stub_body (self, &ctx);
   gum_x86_writer_put_breakpoint (&ctx.cw);
 
   gum_x86_writer_clear (&ctx.cw);
 }
 
 static void
-frida_agent_context_emit_pthread_stub_code (FridaAgentContext * self, guint8 * code, GumDarwinModuleResolver * resolver,
+bads_agent_context_emit_pthread_stub_code (FridaAgentContext * self, guint8 * code, GumDarwinModuleResolver * resolver,
     GumDarwinMapper * mapper)
 {
   FridaAgentEmitContext ctx;
@@ -4425,7 +4425,7 @@ frida_agent_context_emit_pthread_stub_code (FridaAgentContext * self, guint8 * c
   else
     gum_x86_writer_put_mov_reg_reg (&ctx.cw, GUM_X86_XBX, GUM_X86_XDI);
 
-  frida_agent_context_emit_pthread_stub_body (self, &ctx);
+  bads_agent_context_emit_pthread_stub_body (self, &ctx);
 
   gum_x86_writer_put_add_reg_imm (&ctx.cw, GUM_X86_XSP, locals_size);
 
@@ -4452,7 +4452,7 @@ frida_agent_context_emit_pthread_stub_code (FridaAgentContext * self, guint8 * c
     gum_x86_writer_put_call_reg_offset_ptr_with_aligned_arguments (&ctx->cw, GUM_CALL_CAPI, GUM_X86_XBX, G_STRUCT_OFFSET (FridaAgentContext, fun), __VA_ARGS__)
 
 static void
-frida_agent_context_emit_mach_stub_body (FridaAgentContext * self, FridaAgentEmitContext * ctx)
+bads_agent_context_emit_mach_stub_body (FridaAgentContext * self, FridaAgentEmitContext * ctx)
 {
   const gchar * again = "again";
 
@@ -4500,7 +4500,7 @@ frida_agent_context_emit_mach_stub_body (FridaAgentContext * self, FridaAgentEmi
 }
 
 static void
-frida_agent_context_emit_pthread_stub_body (FridaAgentContext * self, FridaAgentEmitContext * ctx)
+bads_agent_context_emit_pthread_stub_body (FridaAgentContext * self, FridaAgentEmitContext * ctx)
 {
   gssize pointer_size, injector_state_offset;
   const gchar * skip_construction = "skip_construction";
@@ -4633,40 +4633,40 @@ frida_agent_context_emit_pthread_stub_body (FridaAgentContext * self, FridaAgent
  * ARM 32- and 64-bit
  */
 
-static void frida_agent_context_emit_arm_mach_stub_code (FridaAgentContext * self, guint8 * code, GumDarwinModuleResolver * resolver,
+static void bads_agent_context_emit_arm_mach_stub_code (FridaAgentContext * self, guint8 * code, GumDarwinModuleResolver * resolver,
     GumDarwinMapper * mapper);
-static void frida_agent_context_emit_arm_pthread_stub_code (FridaAgentContext * self, guint8 * code, GumDarwinModuleResolver * resolver,
+static void bads_agent_context_emit_arm_pthread_stub_code (FridaAgentContext * self, guint8 * code, GumDarwinModuleResolver * resolver,
     GumDarwinMapper * mapper);
-static void frida_agent_context_emit_arm_mach_stub_body (FridaAgentContext * self, FridaAgentEmitContext * ctx);
-static void frida_agent_context_emit_arm_pthread_stub_body (FridaAgentContext * self, FridaAgentEmitContext * ctx);
-static void frida_agent_context_emit_arm_load_reg_with_ctx_value (arm_reg reg, guint field_offset, GumThumbWriter * tw);
-static void frida_agent_context_emit_arm_store_reg_in_ctx_value (guint field_offset, arm_reg reg, GumThumbWriter * tw);
+static void bads_agent_context_emit_arm_mach_stub_body (FridaAgentContext * self, FridaAgentEmitContext * ctx);
+static void bads_agent_context_emit_arm_pthread_stub_body (FridaAgentContext * self, FridaAgentEmitContext * ctx);
+static void bads_agent_context_emit_arm_load_reg_with_ctx_value (arm_reg reg, guint field_offset, GumThumbWriter * tw);
+static void bads_agent_context_emit_arm_store_reg_in_ctx_value (guint field_offset, arm_reg reg, GumThumbWriter * tw);
 
-static void frida_agent_context_emit_arm64_mach_stub_code (FridaAgentContext * self, guint8 * code, GumDarwinModuleResolver * resolver,
+static void bads_agent_context_emit_arm64_mach_stub_code (FridaAgentContext * self, guint8 * code, GumDarwinModuleResolver * resolver,
     GumDarwinMapper * mapper);
-static void frida_agent_context_emit_arm64_pthread_stub_code (FridaAgentContext * self, guint8 * code, GumDarwinModuleResolver * resolver,
+static void bads_agent_context_emit_arm64_pthread_stub_code (FridaAgentContext * self, guint8 * code, GumDarwinModuleResolver * resolver,
     GumDarwinMapper * mapper);
-static void frida_agent_context_emit_arm64_mach_stub_body (FridaAgentContext * self, FridaAgentEmitContext * ctx);
-static void frida_agent_context_emit_arm64_pthread_stub_body (FridaAgentContext * self, FridaAgentEmitContext * ctx);
+static void bads_agent_context_emit_arm64_mach_stub_body (FridaAgentContext * self, FridaAgentEmitContext * ctx);
+static void bads_agent_context_emit_arm64_pthread_stub_body (FridaAgentContext * self, FridaAgentEmitContext * ctx);
 
 static void
-frida_agent_context_emit_mach_stub_code (FridaAgentContext * self, guint8 * code, GumDarwinModuleResolver * resolver,
+bads_agent_context_emit_mach_stub_code (FridaAgentContext * self, guint8 * code, GumDarwinModuleResolver * resolver,
     GumDarwinMapper * mapper)
 {
   if (resolver->cpu_type == GUM_CPU_ARM)
-    frida_agent_context_emit_arm_mach_stub_code (self, code, resolver, mapper);
+    bads_agent_context_emit_arm_mach_stub_code (self, code, resolver, mapper);
   else
-    frida_agent_context_emit_arm64_mach_stub_code (self, code, resolver, mapper);
+    bads_agent_context_emit_arm64_mach_stub_code (self, code, resolver, mapper);
 }
 
 static void
-frida_agent_context_emit_pthread_stub_code (FridaAgentContext * self, guint8 * code, GumDarwinModuleResolver * resolver,
+bads_agent_context_emit_pthread_stub_code (FridaAgentContext * self, guint8 * code, GumDarwinModuleResolver * resolver,
     GumDarwinMapper * mapper)
 {
   if (resolver->cpu_type == GUM_CPU_ARM)
-    frida_agent_context_emit_arm_pthread_stub_code (self, code, resolver, mapper);
+    bads_agent_context_emit_arm_pthread_stub_code (self, code, resolver, mapper);
   else
-    frida_agent_context_emit_arm64_pthread_stub_code (self, code, resolver, mapper);
+    bads_agent_context_emit_arm64_pthread_stub_code (self, code, resolver, mapper);
 }
 
 
@@ -4675,7 +4675,7 @@ frida_agent_context_emit_pthread_stub_code (FridaAgentContext * self, guint8 * c
  */
 
 static void
-frida_agent_context_emit_arm_mach_stub_code (FridaAgentContext * self, guint8 * code, GumDarwinModuleResolver * resolver,
+bads_agent_context_emit_arm_mach_stub_code (FridaAgentContext * self, guint8 * code, GumDarwinModuleResolver * resolver,
     GumDarwinMapper * mapper)
 {
   FridaAgentEmitContext ctx;
@@ -4684,13 +4684,13 @@ frida_agent_context_emit_arm_mach_stub_code (FridaAgentContext * self, guint8 *
   gum_thumb_writer_init (&ctx.tw, ctx.code);
   ctx.mapper = mapper;
 
-  frida_agent_context_emit_arm_mach_stub_body (self, &ctx);
+  bads_agent_context_emit_arm_mach_stub_body (self, &ctx);
 
   gum_thumb_writer_clear (&ctx.tw);
 }
 
 static void
-frida_agent_context_emit_arm_pthread_stub_code (FridaAgentContext * self, guint8 * code, GumDarwinModuleResolver * resolver,
+bads_agent_context_emit_arm_pthread_stub_code (FridaAgentContext * self, guint8 * code, GumDarwinModuleResolver * resolver,
     GumDarwinMapper * mapper)
 {
   FridaAgentEmitContext ctx;
@@ -4701,20 +4701,20 @@ frida_agent_context_emit_arm_pthread_stub_code (FridaAgentContext * self, guint8
 
   gum_thumb_writer_put_push_regs (&ctx.tw, 5, ARM_REG_R4, ARM_REG_R5, ARM_REG_R6, ARM_REG_R7, ARM_REG_LR);
   gum_thumb_writer_put_mov_reg_reg (&ctx.tw, ARM_REG_R7, ARM_REG_R0);
-  frida_agent_context_emit_arm_pthread_stub_body (self, &ctx);
+  bads_agent_context_emit_arm_pthread_stub_body (self, &ctx);
   gum_thumb_writer_put_pop_regs (&ctx.tw, 5, ARM_REG_R4, ARM_REG_R5, ARM_REG_R6, ARM_REG_R7, ARM_REG_PC);
 
   gum_thumb_writer_clear (&ctx.tw);
 }
 
 #define EMIT_ARM_LOAD(reg, field) \
-    frida_agent_context_emit_arm_load_reg_with_ctx_value (ARM_REG_##reg, G_STRUCT_OFFSET (FridaAgentContext, field), &ctx->tw)
+    bads_agent_context_emit_arm_load_reg_with_ctx_value (ARM_REG_##reg, G_STRUCT_OFFSET (FridaAgentContext, field), &ctx->tw)
 #define EMIT_ARM_LOAD_ADDRESS_OF(reg, field) \
     gum_thumb_writer_put_add_reg_reg_imm (&ctx->tw, ARM_REG_##reg, ARM_REG_R7, G_STRUCT_OFFSET (FridaAgentContext, field))
 #define EMIT_ARM_LOAD_U32(reg, val) \
     gum_thumb_writer_put_ldr_reg_u32 (&ctx->tw, ARM_REG_##reg, val)
 #define EMIT_ARM_STORE(field, reg) \
-    frida_agent_context_emit_arm_store_reg_in_ctx_value (G_STRUCT_OFFSET (FridaAgentContext, field), ARM_REG_##reg, &ctx->tw)
+    bads_agent_context_emit_arm_store_reg_in_ctx_value (G_STRUCT_OFFSET (FridaAgentContext, field), ARM_REG_##reg, &ctx->tw)
 #define EMIT_ARM_MOVE(dstreg, srcreg) \
     gum_thumb_writer_put_mov_reg_reg (&ctx->tw, ARM_REG_##dstreg, ARM_REG_##srcreg)
 #define EMIT_ARM_CALL(reg) \
@@ -4723,7 +4723,7 @@ frida_agent_context_emit_arm_pthread_stub_code (FridaAgentContext * self, guint8
     gum_thumb_writer_put_sub_reg_imm (&ctx->tw, ARM_REG_SP, delta * 4)
 
 static void
-frida_agent_context_emit_arm_mach_stub_body (FridaAgentContext * self, FridaAgentEmitContext * ctx)
+bads_agent_context_emit_arm_mach_stub_body (FridaAgentContext * self, FridaAgentEmitContext * ctx)
 {
   const gchar * again = "again";
 
@@ -4765,7 +4765,7 @@ frida_agent_context_emit_arm_mach_stub_body (FridaAgentContext * self, FridaAgen
 }
 
 static void
-frida_agent_context_emit_arm_pthread_stub_body (FridaAgentContext * self, FridaAgentEmitContext * ctx)
+bads_agent_context_emit_arm_pthread_stub_body (FridaAgentContext * self, FridaAgentEmitContext * ctx)
 {
   const gchar * skip_construction = "skip_construction";
   const gchar * skip_dlopen = "skip_dlopen";
@@ -4875,7 +4875,7 @@ frida_agent_context_emit_arm_pthread_stub_body (FridaAgentContext * self, FridaA
 }
 
 static void
-frida_agent_context_emit_arm_load_reg_with_ctx_value (arm_reg reg, guint field_offset, GumThumbWriter * tw)
+bads_agent_context_emit_arm_load_reg_with_ctx_value (arm_reg reg, guint field_offset, GumThumbWriter * tw)
 {
   arm_reg tmp_reg = (reg != ARM_REG_R0) ? ARM_REG_R0 : ARM_REG_R1;
   gum_thumb_writer_put_push_regs (tw, 1, tmp_reg);
@@ -4886,7 +4886,7 @@ frida_agent_context_emit_arm_load_reg_with_ctx_value (arm_reg reg, guint field_o
 }
 
 static void
-frida_agent_context_emit_arm_store_reg_in_ctx_value (guint field_offset, arm_reg reg, GumThumbWriter * tw)
+bads_agent_context_emit_arm_store_reg_in_ctx_value (guint field_offset, arm_reg reg, GumThumbWriter * tw)
 {
   arm_reg tmp_reg = (reg != ARM_REG_R0) ? ARM_REG_R0 : ARM_REG_R1;
   gum_thumb_writer_put_push_regs (tw, 1, tmp_reg);
@@ -4902,7 +4902,7 @@ frida_agent_context_emit_arm_store_reg_in_ctx_value (guint field_offset, arm_reg
  */
 
 static void
-frida_agent_context_emit_arm64_mach_stub_code (FridaAgentContext * self, guint8 * code, GumDarwinModuleResolver * resolver,
+bads_agent_context_emit_arm64_mach_stub_code (FridaAgentContext * self, guint8 * code, GumDarwinModuleResolver * resolver,
     GumDarwinMapper * mapper)
 {
   FridaAgentEmitContext ctx;
@@ -4917,7 +4917,7 @@ frida_agent_context_emit_arm64_mach_stub_code (FridaAgentContext * self, guint8
   gum_arm64_writer_put_mov_reg_reg (&ctx.aw, ARM64_REG_FP, ARM64_REG_SP);
   gum_arm64_writer_put_push_reg_reg (&ctx.aw, ARM64_REG_X19, ARM64_REG_X20);
   gum_arm64_writer_put_push_reg_reg (&ctx.aw, ARM64_REG_X21, ARM64_REG_X22);
-  frida_agent_context_emit_arm64_mach_stub_body (self, &ctx);
+  bads_agent_context_emit_arm64_mach_stub_body (self, &ctx);
   gum_arm64_writer_put_pop_reg_reg (&ctx.aw, ARM64_REG_X21, ARM64_REG_X22);
   gum_arm64_writer_put_pop_reg_reg (&ctx.aw, ARM64_REG_X19, ARM64_REG_X20);
   gum_arm64_writer_put_pop_reg_reg (&ctx.aw, ARM64_REG_FP, ARM64_REG_LR);
@@ -4927,7 +4927,7 @@ frida_agent_context_emit_arm64_mach_stub_code (FridaAgentContext * self, guint8
 }
 
 static void
-frida_agent_context_emit_arm64_pthread_stub_code (FridaAgentContext * self, guint8 * code, GumDarwinModuleResolver * resolver,
+bads_agent_context_emit_arm64_pthread_stub_code (FridaAgentContext * self, guint8 * code, GumDarwinModuleResolver * resolver,
     GumDarwinMapper * mapper)
 {
   FridaAgentEmitContext ctx;
@@ -4942,7 +4942,7 @@ frida_agent_context_emit_arm64_pthread_stub_code (FridaAgentContext * self, guin
   gum_arm64_writer_put_mov_reg_reg (&ctx.aw, ARM64_REG_FP, ARM64_REG_SP);
   gum_arm64_writer_put_push_reg_reg (&ctx.aw, ARM64_REG_X19, ARM64_REG_X20);
   gum_arm64_writer_put_mov_reg_reg (&ctx.aw, ARM64_REG_X20, ARM64_REG_X0);
-  frida_agent_context_emit_arm64_pthread_stub_body (self, &ctx);
+  bads_agent_context_emit_arm64_pthread_stub_body (self, &ctx);
   gum_arm64_writer_put_pop_reg_reg (&ctx.aw, ARM64_REG_X19, ARM64_REG_X20);
   gum_arm64_writer_put_pop_reg_reg (&ctx.aw, ARM64_REG_FP, ARM64_REG_LR);
   gum_arm64_writer_put_ret (&ctx.aw);
@@ -4963,7 +4963,7 @@ frida_agent_context_emit_arm64_pthread_stub_code (FridaAgentContext * self, guin
     gum_arm64_writer_put_blr_reg_no_auth (&ctx->aw, ARM64_REG_##reg)
 
 static void
-frida_agent_context_emit_arm64_mach_stub_body (FridaAgentContext * self, FridaAgentEmitContext * ctx)
+bads_agent_context_emit_arm64_mach_stub_body (FridaAgentContext * self, FridaAgentEmitContext * ctx)
 {
   const gchar * again = "again";
 
@@ -5015,7 +5015,7 @@ frida_agent_context_emit_arm64_mach_stub_body (FridaAgentContext * self, FridaAg
 }
 
 static void
-frida_agent_context_emit_arm64_pthread_stub_body (FridaAgentContext * self, FridaAgentEmitContext * ctx)
+bads_agent_context_emit_arm64_pthread_stub_body (FridaAgentContext * self, FridaAgentEmitContext * ctx)
 {
   const gchar * skip_construction = "skip_construction";
   const gchar * skip_dlopen = "skip_dlopen";
diff --git a/src/darwin/frida-helper-types.vala b/src/darwin/frida-helper-types.vala
index 4a8309d5..e1709e0d 100644
--- a/src/darwin/frida-helper-types.vala
+++ b/src/darwin/frida-helper-types.vala
@@ -46,7 +46,7 @@ namespace Frida {
 		public abstract async MappedLibraryBlob? try_mmap (Bytes blob, Cancellable? cancellable) throws Error, IOError;
 	}
 
-	[DBus (name = "re.frida.Helper")]
+	[DBus (name = "re.fs.Helper")]
 	public interface DarwinRemoteHelper : Object {
 		public signal void output (uint pid, int fd, uint8[] data);
 		public signal void spawn_added (HostSpawnInfo info);
@@ -126,7 +126,7 @@ namespace Frida {
 	}
 
 	namespace ObjectPath {
-		public const string HELPER = "/re/frida/Helper";
-		public const string SYSTEM_SESSION_PROVIDER = "/re/frida/SystemSessionProvider";
+		public const string HELPER = "/re/fs/Helper";
+		public const string SYSTEM_SESSION_PROVIDER = "/re/fs/SystemSessionProvider";
 	}
 }
diff --git a/src/darwin/frida-helper.plist b/src/darwin/frida-helper.plist
index 46c64f67..be000a90 100644
--- a/src/darwin/frida-helper.plist
+++ b/src/darwin/frida-helper.plist
@@ -5,7 +5,7 @@
 	<key>CFBundleDevelopmentRegion</key>
 	<string>English</string>
 	<key>CFBundleIdentifier</key>
-	<string>re.frida.Helper</string>
+	<string>re.fs.Helper</string>
 	<key>CFBundleInfoDictionaryVersion</key>
 	<string>6.0</string>
 	<key>CFBundleName</key>
diff --git a/src/darwin/frida-helper.xcent b/src/darwin/frida-helper.xcent
index 01a54e6b..087346a0 100644
--- a/src/darwin/frida-helper.xcent
+++ b/src/darwin/frida-helper.xcent
@@ -7,7 +7,7 @@
 	<key>adi-client</key>
 	<string>166430402</string>
 	<key>application-identifier</key>
-	<string>re.frida.Helper</string>
+	<string>re.fs.Helper</string>
 	<key>aps-connection-initiate</key>
 	<true/>
 	<key>auth-key-vault-client</key>
diff --git a/src/darwin/policyd.h b/src/darwin/policyd.h
index b92cad28..a8f607cc 100644
--- a/src/darwin/policyd.h
+++ b/src/darwin/policyd.h
@@ -3,7 +3,7 @@
 
 #include "policyd-client.h"
 
-#define FRIDA_POLICYD_SERVICE_NAME "re.frida.policyd"
+#define FRIDA_POLICYD_SERVICE_NAME "re.fs.policyd"
 
 int frida_policyd_main (void);
 
diff --git a/src/droidy/droidy-host-session.vala b/src/droidy/droidy-host-session.vala
index 7f6342ff..ff5f4108 100644
--- a/src/droidy/droidy-host-session.vala
+++ b/src/droidy/droidy-host-session.vala
@@ -246,7 +246,7 @@ namespace Frida {
 		private Cancellable io_cancellable = new Cancellable ();
 
 		private const double MIN_SERVER_CHECK_INTERVAL = 5.0;
-		private const string GADGET_APP_ID = "re.frida.Gadget";
+		private const string GADGET_APP_ID = "re.fs.Gadget";
 
 		public DroidyHostSession (Droidy.DeviceDetails device_details, HostChannelProvider channel_provider) {
 			Object (
@@ -526,7 +526,7 @@ namespace Frida {
 				if (opts.has_selected_identifiers ()) {
 					gadget_is_selected = false;
 					opts.enumerate_selected_identifiers (identifier => {
-						if (identifier == "re.frida.Gadget")
+						if (identifier == "re.fs.Gadget")
 							gadget_is_selected = true;
 					});
 				}
@@ -1239,8 +1239,8 @@ namespace Frida {
 
 					shell.send_command (("CLASSPATH=%s app_process " +
 							"/data/local/tmp " +
-							"--nice-name=re.frida.helper " +
-							"re.frida.Helper " +
+							"--nice-name=re.fs.helper " +
+							"re.fs.Helper " +
 							"%s; " +
 							"rm -f %s; " +
 							"echo BYE.").printf (helper_path, instance_id, helper_path));
@@ -1552,7 +1552,7 @@ namespace Frida {
 			}
 		}
 
-		/* Compiled from helper/re/frida/Helper.java */
+		/* Compiled from helper/re/fs/Helper.java */
 		private const uint8[] HELPER_DEX = {
 			0x64, 0x65, 0x78, 0x0a, 0x30, 0x33, 0x35, 0x00, 0x95, 0x6c, 0x1e, 0xee, 0xe7, 0x7d, 0x5d, 0xfc, 0xbd, 0x43, 0x1a,
 			0x78, 0x94, 0x6e, 0x5e, 0x0f, 0x96, 0x9b, 0x3b, 0x62, 0xed, 0x6a, 0x94, 0xc3, 0x38, 0x41, 0x00, 0x00, 0x70, 0x00,
diff --git a/src/droidy/helper/Makefile b/src/droidy/helper/Makefile
index e295788e..e888ddc7 100644
--- a/src/droidy/helper/Makefile
+++ b/src/droidy/helper/Makefile
@@ -17,4 +17,4 @@ build/frida-helper.jar: re/frida/Helper.java
 		-Xlint:unchecked \
 		$^ \
 		-d build/java/
-	jar cfe $@ re.frida.Helper -C build/java/ .
+	jar cfe $@ re.fs.Helper -C build/java/ .
diff --git a/src/droidy/helper/re/frida/Helper.java b/src/droidy/helper/re/frida/Helper.java
index d0002311..7eec124f 100644
--- a/src/droidy/helper/re/frida/Helper.java
+++ b/src/droidy/helper/re/frida/Helper.java
@@ -1,4 +1,4 @@
-package re.frida;
+package re.fs;
 
 import android.app.ActivityManager;
 import android.app.ActivityManager.RunningAppProcessInfo;
diff --git a/src/embed-agent.py b/src/embed-agent.py
index cfb4a594..41427636 100644
--- a/src/embed-agent.py
+++ b/src/embed-agent.py
@@ -33,7 +33,7 @@ def main(argv):
             if agent is None:
                 continue
             arch = detect_pefile_arch(agent)
-            embedded_agent = priv_dir / f"frida-agent-{arch}.dll"
+            embedded_agent = priv_dir / f"bads-agent-{arch}.dll"
             embedded_dbghelp = priv_dir / f"dbghelp-{arch}.dll"
             embedded_symsrv = priv_dir / f"symsrv-{arch}.dll"
 
@@ -52,14 +52,14 @@ def main(argv):
             embedded_assets += [embedded_agent, embedded_dbghelp, embedded_symsrv]
             pending_archs.remove(arch)
         for missing_arch in pending_archs:
-            embedded_agent = priv_dir / f"frida-agent-{missing_arch}.dll"
+            embedded_agent = priv_dir / f"bads-agent-{missing_arch}.dll"
             embedded_dbghelp = priv_dir / f"dbghelp-{missing_arch}.dll"
             embedded_symsrv = priv_dir / f"symsrv-{missing_arch}.dll"
             for asset in {embedded_agent, embedded_dbghelp, embedded_symsrv}:
                 asset.write_bytes(b"")
                 embedded_assets += [asset]
     elif host_os in {"macos", "ios", "watchos", "tvos", "xros"}:
-        embedded_agent = priv_dir / "frida-agent.dylib"
+        embedded_agent = priv_dir / "bads-agent.dylib"
         if agent_modern is not None and agent_legacy is not None:
             subprocess.run(lipo + [agent_modern, agent_legacy, "-create", "-output", embedded_agent],
                            check=True)
@@ -73,14 +73,14 @@ def main(argv):
                               (agent_legacy, "32"),
                               (agent_emulated_modern, "arm64"),
                               (agent_emulated_legacy, "arm")]:
-            embedded_agent = priv_dir / f"frida-agent-{flavor}.so"
+            embedded_agent = priv_dir / f"bads-agent-{flavor}.so"
             if agent is not None:
                 shutil.copy(agent, embedded_agent)
             else:
                 embedded_agent.write_bytes(b"")
             embedded_assets += [embedded_agent]
     elif host_os in {"freebsd", "qnx"}:
-        embedded_agent = priv_dir / "frida-agent.so"
+        embedded_agent = priv_dir / "bads-agent.so"
         agent = agent_modern if agent_modern is not None else agent_legacy
         shutil.copy(agent, embedded_agent)
         embedded_assets += [embedded_agent]
@@ -93,7 +93,7 @@ def main(argv):
         f"--toolchain={host_toolchain}",
         f"--machine={host_arch}",
         "--config-filename", resource_config,
-        "--output-basename", output_dir / "frida-data-agent",
+        "--output-basename", output_dir / "bads-data-agent",
     ] + embedded_assets, check=True)
 
 
diff --git a/src/freebsd/freebsd-host-session.vala b/src/freebsd/freebsd-host-session.vala
index 245747ab..483c8d63 100644
--- a/src/freebsd/freebsd-host-session.vala
+++ b/src/freebsd/freebsd-host-session.vala
@@ -25,7 +25,7 @@ namespace Frida {
 			binjector.uninjected.connect (on_uninjected);
 			injector = binjector;
 
-			var blob = Frida.Data.Agent.get_frida_agent_so_blob ();
+			var blob = Frida.Data.Agent.get_bads_agent_so_blob ();
 			agent_desc = new AgentDescriptor (blob.name, new MemoryInputStream.from_data (blob.data, null));
 		}
 
@@ -132,7 +132,7 @@ namespace Frida {
 
 			var stream_request = Pipe.open (t.local_address, cancellable);
 
-			var id = yield binjector.inject_library_resource (pid, agent_desc, "frida_agent_main",
+			var id = yield binjector.inject_library_resource (pid, agent_desc, "bads_agent_main",
 				make_agent_parameters (pid, t.remote_address, options), cancellable);
 			injectee_by_pid[pid] = id;
 
diff --git a/src/fruity/device-monitor-macos.vala b/src/fruity/device-monitor-macos.vala
index 063af220..c5cc170c 100644
--- a/src/fruity/device-monitor-macos.vala
+++ b/src/fruity/device-monitor-macos.vala
@@ -8,7 +8,7 @@ namespace Frida.Fruity {
 
 		private XpcClient? pairingd;
 		private Darwin.GCD.DispatchQueue queue =
-			new Darwin.GCD.DispatchQueue ("re.frida.fruity.remotepairing", Darwin.GCD.DispatchQueueAttr.SERIAL);
+			new Darwin.GCD.DispatchQueue ("re.fs.fruity.remotepairing", Darwin.GCD.DispatchQueueAttr.SERIAL);
 
 		public async void start (Cancellable? cancellable) throws IOError {
 			pairingd = XpcClient.make_for_mach_service ("com.apple.CoreDevice.remotepairingd", queue);
diff --git a/src/fruity/fruity-host-session.vala b/src/fruity/fruity-host-session.vala
index 1d78d992..8426cc20 100644
--- a/src/fruity/fruity-host-session.vala
+++ b/src/fruity/fruity-host-session.vala
@@ -192,7 +192,7 @@ namespace Frida {
 		private Cancellable io_cancellable = new Cancellable ();
 
 		private const double MIN_SERVER_CHECK_INTERVAL = 5.0;
-		private const string GADGET_APP_ID = "re.frida.Gadget";
+		private const string GADGET_APP_ID = "re.fs.Gadget";
 		private const string DEBUGSERVER_ENDPOINT_17PLUS = "com.apple.internal.dt.remote.debugproxy";
 		private const string DEBUGSERVER_ENDPOINT_14PLUS = "com.apple.debugserver.DVTSecureSocketProxy";
 		private const string DEBUGSERVER_ENDPOINT_LEGACY = "com.apple.debugserver?tls=handshake-only";
diff --git a/src/host-session-service.vala b/src/host-session-service.vala
index b7a0c167..3202e45b 100644
--- a/src/host-session-service.vala
+++ b/src/host-session-service.vala
@@ -614,7 +614,7 @@ namespace Frida {
 				promise.resolve (entry);
 			} catch (GLib.Error e) {
 				if (e is IOError.CANCELLED && (cancellable == null || !cancellable.is_cancelled ())) {
-					e = new Error.PROCESS_NOT_RESPONDING ("Process with pid %u either refused to load frida-agent, " +
+					e = new Error.PROCESS_NOT_RESPONDING ("Process with pid %u either refused to load bads-agent, " +
 						"or terminated during injection", pid);
 				}
 
diff --git a/src/linux/frida-helper-types.vala b/src/linux/frida-helper-types.vala
index 8d19c279..01abb71a 100644
--- a/src/linux/frida-helper-types.vala
+++ b/src/linux/frida-helper-types.vala
@@ -42,7 +42,7 @@ namespace Frida {
 		CONTROL_CHANNEL,
 	}
 
-	[DBus (name = "re.frida.Helper")]
+	[DBus (name = "re.fs.Helper")]
 	public interface LinuxRemoteHelper : Object {
 		public signal void output (uint pid, int fd, uint8[] data);
 		public signal void uninjected (uint id);
@@ -88,6 +88,6 @@ namespace Frida {
 	}
 
 	namespace ObjectPath {
-		public const string HELPER = "/re/frida/Helper";
+		public const string HELPER = "/re/fs/Helper";
 	}
 }
diff --git a/src/linux/linux-host-session.vala b/src/linux/linux-host-session.vala
index 55a0fbec..0c8918c1 100644
--- a/src/linux/linux-host-session.vala
+++ b/src/linux/linux-host-session.vala
@@ -58,16 +58,16 @@ namespace Frida {
 			injector.uninjected.connect (on_uninjected);
 
 #if HAVE_EMBEDDED_ASSETS
-			var blob32 = Frida.Data.Agent.get_frida_agent_32_so_blob ();
-			var blob64 = Frida.Data.Agent.get_frida_agent_64_so_blob ();
-			var emulated_arm = Frida.Data.Agent.get_frida_agent_arm_so_blob ();
-			var emulated_arm64 = Frida.Data.Agent.get_frida_agent_arm64_so_blob ();
-			agent = new AgentDescriptor (PathTemplate ("frida-agent-<arch>.so"),
+			var blob32 = Frida.Data.Agent.get_bads_agent_32_so_blob ();
+			var blob64 = Frida.Data.Agent.get_bads_agent_64_so_blob ();
+			var emulated_arm = Frida.Data.Agent.get_bads_agent_arm_so_blob ();
+			var emulated_arm64 = Frida.Data.Agent.get_bads_agent_arm64_so_blob ();
+			agent = new AgentDescriptor (PathTemplate ("bads-agent-<arch>.so"),
 				new Bytes.static (blob32.data),
 				new Bytes.static (blob64.data),
 				new AgentResource[] {
-					new AgentResource ("frida-agent-arm.so", new Bytes.static (emulated_arm.data), tempdir),
-					new AgentResource ("frida-agent-arm64.so", new Bytes.static (emulated_arm64.data), tempdir),
+					new AgentResource ("bads-agent-arm.so", new Bytes.static (emulated_arm.data), tempdir),
+					new AgentResource ("bads-agent-arm64.so", new Bytes.static (emulated_arm64.data), tempdir),
 				},
 				AgentMode.INSTANCED,
 				tempdir);
@@ -360,7 +360,7 @@ namespace Frida {
 		protected override async Future<IOStream> perform_attach_to (uint pid, HashTable<string, Variant> options,
 				Cancellable? cancellable, out Object? transport) throws Error, IOError {
 			uint id;
-			string entrypoint = "frida_agent_main";
+			string entrypoint = "bads_agent_main";
 			string parameters = make_agent_parameters (pid, "", options);
 			AgentFeatures features = CONTROL_CHANNEL;
 			var linjector = (Linjector) injector;
@@ -385,10 +385,10 @@ namespace Frida {
 			unowned string name;
 			switch (cpu_type_from_pid (pid)) {
 				case Gum.CpuType.IA32:
-					name = "frida-agent-arm.so";
+					name = "bads-agent-arm.so";
 					break;
 				case Gum.CpuType.AMD64:
-					name = "frida-agent-arm64.so";
+					name = "bads-agent-arm64.so";
 					break;
 				default:
 					throw new Error.NOT_SUPPORTED ("Emulated realm is not supported on this architecture");
diff --git a/src/linux/system-linux.c b/src/linux/system-linux.c
index 7366d698..183ea782 100644
--- a/src/linux/system-linux.c
+++ b/src/linux/system-linux.c
@@ -165,7 +165,7 @@ frida_temporary_directory_get_system_tmp (void)
 
   /*
    * If the temporary directory resides on a file-system which is marked
-   * `noexec`, then we won't be able to write the frida-agent.so there and
+   * `noexec`, then we won't be able to write the bads-agent.so there and
    * subsequently dlopen() it inside the target application as it will result in
    * permission denied.
    *
diff --git a/src/meson.build b/src/meson.build
index 212409aa..d8fdf5f2 100644
--- a/src/meson.build
+++ b/src/meson.build
@@ -30,7 +30,7 @@ backend_libs = []
 backend_deps = []
 
 if have_local_backend
-  post_process_helper = post_process + ['executable', 're.frida.Helper']
+  post_process_helper = post_process + ['executable', 're.fs.Helper']
 
   embed_helper = [
     python,
@@ -469,13 +469,13 @@ if have_local_backend
       agent_emulated_legacy = ''
     endif
 
-    backend_sources += custom_target('frida-data-agent',
+    backend_sources += custom_target('bads-data-agent',
       input: 'agent.resources',
       output: [
-        'frida-data-agent-blob' + resource_blob_extension,
-        'frida-data-agent.vapi',
-        'frida-data-agent.h',
-        'frida-data-agent.c',
+        'bads-data-agent-blob' + resource_blob_extension,
+        'bads-data-agent.vapi',
+        'bads-data-agent.h',
+        'bads-data-agent.c',
       ],
       command: [
         python,
diff --git a/src/qnx/qnx-host-session.vala b/src/qnx/qnx-host-session.vala
index 7d5b3e51..96b94ba4 100644
--- a/src/qnx/qnx-host-session.vala
+++ b/src/qnx/qnx-host-session.vala
@@ -23,7 +23,7 @@ namespace Frida {
 			injector = new Qinjector ();
 			injector.uninjected.connect (on_uninjected);
 
-			var blob = Frida.Data.Agent.get_frida_agent_so_blob ();
+			var blob = Frida.Data.Agent.get_bads_agent_so_blob ();
 			agent_desc = new AgentDescriptor (blob.name, new MemoryInputStream.from_data (blob.data, null));
 		}
 
@@ -117,7 +117,7 @@ namespace Frida {
 
 			var stream_request = Pipe.open (t.local_address, cancellable);
 
-			var id = yield qinjector.inject_library_resource (pid, agent_desc, "frida_agent_main",
+			var id = yield qinjector.inject_library_resource (pid, agent_desc, "bads_agent_main",
 				make_agent_parameters (pid, t.remote_address, options), cancellable);
 			injectee_by_pid[pid] = id;
 
diff --git a/src/windows/frida-helper-types.vala b/src/windows/frida-helper-types.vala
index 0d410202..2bdc688f 100644
--- a/src/windows/frida-helper-types.vala
+++ b/src/windows/frida-helper-types.vala
@@ -8,7 +8,7 @@ namespace Frida {
 			string[] dependencies, uint id, Cancellable? cancellable) throws Error, IOError;
 	}
 
-	[DBus (name = "re.frida.Helper")]
+	[DBus (name = "re.fs.Helper")]
 	public interface WindowsRemoteHelper : Object {
 		public signal void uninjected (uint id);
 
@@ -44,6 +44,6 @@ namespace Frida {
 	}
 
 	namespace ObjectPath {
-		public const string HELPER = "/re/frida/Helper";
+		public const string HELPER = "/re/fs/Helper";
 	}
 }
diff --git a/src/windows/windows-host-session.vala b/src/windows/windows-host-session.vala
index 1f9c32eb..5ae36252 100644
--- a/src/windows/windows-host-session.vala
+++ b/src/windows/windows-host-session.vala
@@ -46,10 +46,10 @@ namespace Frida {
 			injector = new Winjector (helper, false, tempdir);
 			injector.uninjected.connect (on_uninjected);
 
-			agent = new AgentDescriptor (PathTemplate ("<arch>\\frida-agent.dll"),
-				new Bytes.static (Frida.Data.Agent.get_frida_agent_arm64_dll_blob ().data),
-				new Bytes.static (Frida.Data.Agent.get_frida_agent_x86_64_dll_blob ().data),
-				new Bytes.static (Frida.Data.Agent.get_frida_agent_x86_dll_blob ().data),
+			agent = new AgentDescriptor (PathTemplate ("<arch>\\bads-agent.dll"),
+				new Bytes.static (Frida.Data.Agent.get_bads_agent_arm64_dll_blob ().data),
+				new Bytes.static (Frida.Data.Agent.get_bads_agent_x86_64_dll_blob ().data),
+				new Bytes.static (Frida.Data.Agent.get_bads_agent_x86_dll_blob ().data),
 				new AgentResource[] {
 					new AgentResource ("arm64\\dbghelp.dll",
 						new Bytes.static (Frida.Data.Agent.get_dbghelp_arm64_dll_blob ().data), tempdir),
@@ -225,7 +225,7 @@ namespace Frida {
 			var stream_request = Pipe.open (t.local_address, cancellable);
 
 			var winjector = injector as Winjector;
-			var id = yield winjector.inject_library_resource (pid, agent, "frida_agent_main",
+			var id = yield winjector.inject_library_resource (pid, agent, "bads_agent_main",
 				make_agent_parameters (pid, t.remote_address, options), cancellable);
 			injectee_by_pid[pid] = id;
 
diff --git a/tests/frida-tests.plist b/tests/frida-tests.plist
index 5340a45a..0476feac 100644
--- a/tests/frida-tests.plist
+++ b/tests/frida-tests.plist
@@ -5,7 +5,7 @@
 	<key>CFBundleDevelopmentRegion</key>
 	<string>English</string>
 	<key>CFBundleIdentifier</key>
-	<string>re.frida.CoreTests</string>
+	<string>re.fs.CoreTests</string>
 	<key>CFBundleInfoDictionaryVersion</key>
 	<string>6.0</string>
 	<key>CFBundleName</key>
diff --git a/tests/frida-tests.xcent b/tests/frida-tests.xcent
index 4401a530..1807a787 100644
--- a/tests/frida-tests.xcent
+++ b/tests/frida-tests.xcent
@@ -13,7 +13,7 @@
 	<key>adi-client</key>
 	<string>166430402</string>
 	<key>application-identifier</key>
-	<string>re.frida.Tests</string>
+	<string>re.fs.Tests</string>
 	<key>aps-connection-initiate</key>
 	<true/>
 	<key>auth-key-vault-client</key>
diff --git a/tests/meson.build b/tests/meson.build
index 37065dfb..35ef2b4d 100644
--- a/tests/meson.build
+++ b/tests/meson.build
@@ -88,7 +88,7 @@ raw_runner = executable('frida-tests-raw', 'main.vala',
 runner = custom_target('frida-tests',
   input: [raw_runner, 'frida-tests.xcent'],
   output: 'frida-tests' + exe_suffix,
-  command: post_process + ['executable', 're.frida.CoreTests', '@INPUT1@'],
+  command: post_process + ['executable', 're.fs.CoreTests', '@INPUT1@'],
   build_by_default: true,
 )
 
diff --git a/tools/package-server-fruity.sh b/tools/package-server-fruity.sh
index 48cc35e9..8d93e204 100755
--- a/tools/package-server-fruity.sh
+++ b/tools/package-server-fruity.sh
@@ -59,7 +59,7 @@ mkdir -p "$daedir/"
   echo '<plist version="1.0">'
   echo "<dict>"
   echo "	<key>Label</key>"
-  echo "	<string>re.frida.server</string>"
+  echo "	<string>re.fs.server</string>"
   echo "	<key>Program</key>"
   echo "	<string>$sysroot/usr/sbin/frida-server</string>"
   echo "	<key>ProgramArguments</key>"
@@ -91,14 +91,14 @@ mkdir -p "$daedir/"
   echo "	<true/>"
   echo "</dict>"
   echo "</plist>"
-) > "$daedir/re.frida.server.plist"
-chmod 644 "$daedir/re.frida.server.plist"
+) > "$daedir/re.fs.server.plist"
+chmod 644 "$daedir/re.fs.server.plist"
 
 installed_size=$(du -sk "$tmpdir" | cut -f1)
 
 mkdir -p "$tmpdir/DEBIAN/"
 cat >"$tmpdir/DEBIAN/control" <<EOF
-Package: re.frida.server
+Package: re.fs.server
 Name: Frida
 Version: $FRIDA_VERSION
 Priority: optional
@@ -110,14 +110,14 @@ Homepage: https://frida.re/
 Maintainer: Ole André Vadla Ravnås <oleavr@nowsecure.com>
 Author: Frida Developers <oleavr@nowsecure.com>
 Section: Development
-Conflicts: re.frida.server64
+Conflicts: re.fs.server64
 EOF
 chmod 644 "$tmpdir/DEBIAN/control"
 
 cat >"$tmpdir/DEBIAN/extrainst_" <<EOF
 #!/bin/bash
 
-launchcfg=$sysroot/Library/LaunchDaemons/re.frida.server.plist
+launchcfg=$sysroot/Library/LaunchDaemons/re.fs.server.plist
 launchlog=\$(mktemp)
 
 function dispose {
@@ -152,7 +152,7 @@ cat >"$tmpdir/DEBIAN/prerm" <<EOF
 #!/bin/bash
 
 if [ "\$1" = remove ] || [ "\$1" = purge ]; then
-  launchctl unload $sysroot/Library/LaunchDaemons/re.frida.server.plist &> /dev/null
+  launchctl unload $sysroot/Library/LaunchDaemons/re.fs.server.plist &> /dev/null
 fi
 
 exit 0
